apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: banking
spec:
  selector:
    app: banking-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
